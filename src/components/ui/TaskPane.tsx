'use client'
// Hollow starter — implement after data shape is confirmed
export default function TaskPane({ children }: { children: React.ReactNode }) {
  return (
    <div className="w-full h-full flex flex-col">
      {children}
    </div>
  )
}
